# ============================================================================
# Environment Variables Template
# ============================================================================
# 
# IMPORTANT SECURITY NOTES:
# 1. This file contains PLACEHOLDER values only - never commit real secrets
# 2. For Lovable Cloud projects, .env is auto-managed (do not edit manually)
# 3. The Supabase anon key is a publishable key (safe for client-side use)
# 4. Real security comes from Row Level Security (RLS) policies
# 5. NEVER expose service_role keys client-side (not listed here)
#
# For local development in non-Cloud projects:
# - Copy this to .env.local (untracked by Git)
# - Fill in actual values from your Supabase project dashboard
#
# ============================================================================

# Supabase Project Configuration
VITE_SUPABASE_PROJECT_ID="<your-project-ref>"
VITE_SUPABASE_URL="https://<your-project-ref>.supabase.co"
VITE_SUPABASE_PUBLISHABLE_KEY="<your-anon-key>"

# ============================================================================
# SECURITY CHECKLIST:
# ============================================================================
# [ ] Verified RLS policies are enabled on all tables
# [ ] Reviewed RLS policies for least-privilege access
# [ ] Confirmed no service_role keys in client code
# [ ] Storage bucket policies properly configured
# [ ] Rate limiting configured for public endpoints
# [ ] Rotated anon key if it was previously exposed in VCS
# ============================================================================
